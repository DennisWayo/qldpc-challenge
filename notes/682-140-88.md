# [[682,140,88]] - cyclic-ideal generalized-bicycle code

Distance is a witness-backed upper bound (`d <= 88`), not an exact-distance
claim.

## Direction

This entry targets the unrestricted / weight-9plus board. At search time the
score to beat was `1573.977` from `[[682,172,79]]`, so a `[[682,140,88]]`
upper-bound submission scores

`140 * 88^2 / 682 = 1589.677`.

The candidate is a length-682 cyclic generalized-bicycle code over `Z_341` with
dimension `k = 140`, maximum check weight `30`, and fingerprint
`a76bd7f863b99c7e`.

## Construction

Let `A` and `B` be length-341 binary circulants over `Z_341`. Use first-row
supports:

`a = [0, 1, 11, 15, 22, 32, 110, 132, 139, 170, 176, 187, 264, 308, 325]`

`b = [42, 82, 87, 97, 116, 155, 157, 170, 178, 217, 244, 257, 267, 283, 300]`

Build the CSS checks as `H_X = [circ(a) | circ(b)]` and
`H_Z = [circ(b)^T | circ(a)^T]`. The sparse parity-check supports and both
distance witnesses are recorded in `codes/682-140-88.json`.

The cyclic-ideal miner produced this record from a degree-70 divisor ideal of
`x^341 + 1` over GF(2), with divisor polynomial `0x725946e001e3c38d9d`.

## Evidence

This candidate was rank 41 in the frozen private snapshot
`20260910T115734-ranks33-64-careful-attack`, then passed a deeper private
attack in `20260910T122604-ranks40-42-ultra-attack`.

The deeper pass ran:

- 20 structure-aware circulant GB passes at 500,000 trials each.
- 10 full-kernel randomized passes at 200,000 trials each.
- No validated witness below weight 88 was found.
- The deep pass found 23 X-side weight-88 witnesses and 3 Z-side weight-88
  witnesses.

The submitted witnesses are:

- X: weight 88, from `gf2_fast.circulant_gb_witness`, seed `24361033`.
- Z: weight 88, from `gf2_fast.distance_rand_witness`, seed `74361033`.

Local validation:

- `verify/qldpc_verify.py` accepts the JSON: CSS commutation, computed
  `k = 140`, max check weight `30`, valid X/Z witnesses, and no 8,000-trial
  verifier refutation.
- `verify/validate_candidate.py` accepts the JSON: board-advancing for
  `weight-9plus x unrestricted`, no exact duplicate, no local WL-equivalent
  entry, and no lighter logical found in its local refutation pass.

## Follow-up

The exact distance has not been proved. This submission should be treated as a
clean upper-bound entry first; exact-distance certification remains separate
follow-up work.

## Tools

The search used the repository verifier stack, `verify/gf2_fast.cpp` for fast
GF(2) randomized witness search, a private cyclic-ideal generalized-bicycle
miner, a private structural/full-kernel attack harness, and
`verify/validate_candidate.py` for the local candidate gate.

This PR was supported by OpenAI Codex (GPT-5).
